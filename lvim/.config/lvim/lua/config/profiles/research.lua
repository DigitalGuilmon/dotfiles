return {
  -- Research/writing profile for notebook, REPL, LaTeX, and CSV-heavy work.
  features = {
    csv_tools = true,
    latex_tools = true,
    notebook_tools = true,
    repl_tools = true,
  },
}
